{
  "name": "vscode-quarkus-dji-extension",
  "description": "Image classifier VS Code extension",
  "version": "0.0.1",
  "license": "Apache-2.0",
  "publisher": "caponetto",
  "engines": {
    "vscode": "^1.43.0"
  },
  "activationEvents": [
    "*"
  ],
  "contributes": {
    "commands": [
      {
        "command": "extension.command.classify",
        "title": "Classify"
      }
    ],
    "menus": {
      "commandPalette": [
        {
          "when": "serverUp && resourceExtname =~ /.jpg|.jpeg|.png/",
          "command": "extension.command.classify"
        }
      ],
      "editor/title": [
        {
          "when": "serverUp && resourceExtname =~ /.jpg|.jpeg|.png/",
          "command": "extension.command.classify",
          "group": "navigation"
        }
      ]
    }
  },
  "main": "./dist/extension/extension.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/caponetto/vscode-quarkus-dji.git"
  },
  "scripts": {
    "package:prod": "cross-var \"vsce package --yarn -o ./dist/image_classifier_vscode_extension_$npm_package_version.vsix\"",
    "compile": "webpack",
    "watch": "webpack",
    "build:fast": "rm -rf dist && webpack",
    "build": "yarn run build:fast",
    "build:prod:linux:darwin": "yarn run build --mode production --devtool none && yarn run package:prod",
    "build:prod:windows": "yarn run build --mode production --devtool none && yarn run package:prod",
    "build:prod": "run-script-os"
  },
  "dependencies": {
    "@kogito-tooling/backend": "0.7.0",
    "vscode-quarkus-djl-app-server": "0.0.1"
  },
  "babel": {
    "presets": [
      "@babel/env",
      "@babel/react"
    ]
  }
}
