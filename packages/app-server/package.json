{
  "name": "vscode-quarkus-djl-app-server",
  "version": "0.0.0",
  "description": "",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/caponetto/vscode-quarkus-djl.git"
  },
  "scripts": {
    "build:dev": "mvn clean package -B -nsu -DskipTests",
    "build:prod": "run-script-os",
    "build:prod:linux": "mvn clean package -B -nsu",
    "build:prod:win32:darwin": "mvn clean package -B -nsu -DskipTests"
  }
}
